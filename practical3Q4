#include<stdio.h>
int main()
{
    float radius;
    const float PI=3.14159;

    printf("enetr the radius of the circle");
    scanf("%f",&radius);

    printf("diamter:%f\n",2*radius);
    printf("circumference:%f\n",2*PI*radius);
    printf("area:%f\n",PI*radius*radius);
    
}

